# Jettx

Corporate site with the Jettx logo and an on-page inbox.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). Compose stores messages in the browser for now.
